import{useState}from'react';import{Sparkles,Send,Loader2,Check,ArrowRight,BarChart3,RotateCcw}from'lucide-react';import{Link}from'react-router-dom';
export default function Agent({trip,onExpenseAdded,onTripChanged}:{trip:any,onExpenseAdded:(expense:any)=>void,onTripChanged:(trip:any)=>void}){const[messages,setMessages]=useState<any[]>([{role:'agent',text:`Hey! I'm your Splitmate Agent. Tell me what happened and I'll help you record it, understand your spending, or work out the settlement.`}]);const[input,setInput]=useState('');const[loading,setLoading]=useState(false);const[conversationId,setConversationId]=useState<string|null>(null);const[activity,setActivity]=useState('Ready to help');
const send=async(override?:string)=>{const text=(override??input).trim();if(!text||loading)return;const history=messages.map(m=>({role:m.role==='agent'?'assistant':'user',content:m.text}));setInput('');setMessages((m)=>[...m,{role:'user',text}]);setLoading(true);setActivity('Understanding your group…');try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,history,trip,conversationId})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Agent request failed');if(data.conversationId)setConversationId(data.conversationId);setMessages(m=>[...m,{role:'agent',text:data.message||'Done.',action:data.action}]);setActivity(data.action?.type==='add_expense'?'Expense ready to review':data.action?.type==='show_settlement'?'Settlement ready':data.action?.type==='analyze_spending'?'Spending analysis ready':'Ready for the next message')}catch{setMessages(m=>[...m,{role:'agent',text:'I lost the thread there. Tell me what happened again and I’ll work through it with you, one detail at a time.'}]);setActivity('Needs a clearer message')}finally{setLoading(false)}};
const confirm=(a:any)=>{if(a.type==='add_expense')onExpenseAdded({title:a.title,amount:Number(a.amount),paid:a.paidBy,split:a.splitBetween});else if(a.type==='update_expense'){const expenses=trip.expenses.map((e:any,i:number)=>i===a.expenseIndex?{...e,...a,paid:a.paidBy??e.paid,split:a.splitBetween??e.split}:e);onTripChanged({...trip,expenses})}else if(a.type==='delete_expense')onTripChanged({...trip,expenses:trip.expenses.filter((_:any,i:number)=>i!==a.expenseIndex)});setMessages(m=>m.map(x=>x.action===a?{...x,action:{...a,confirmed:true}}:x));setActivity('Group updated');};
const reset=()=>{setMessages([{role:'agent',text:`Conversation reset. I still know this group has ${trip.people.length} people and ${trip.expenses.length} expenses. What happened?`}]);setConversationId(null);setActivity('Ready to help')};const person=(id:string)=>trip.people.find((p:any)=>p.id===id)?.name||'Unknown';const total=(trip.expenses||[]).reduce((s:number,e:any)=>s+Number(e.amount),0);const biggest=(trip.expenses||[]).reduce((x:any,e:any)=>!x||Number(e.amount)>Number(x.amount)?e:x,null);return <div className="agent-card"><div className="agent-head"><div className="agent-mark"><Sparkles size={17}/></div><div style={{flex:1}}><b>Splitmate Agent</b><small>Shared-money assistant</small></div><span className="online">● Online</span></div><div className="agent-status"><span>{loading?'Working through the group data…':activity}</span></div><div className="agent-context"><span>{trip.people.length} people</span><span>·</span><span>{trip.expenses.length} expenses</span><span>·</span><span>${total.toFixed(2)} shared</span></div><div className="agent-messages">{messages.map((m,i)=><div key={i} className={`bubble ${m.role==='user'?'user':''}`}><div>{m.text}</div>{m.action?.type==='add_expense'&&!m.action.confirmed&&<div className="agent-confirm"><b>Expense ready</b><p>{m.action.title} · ${Number(m.action.amount).toFixed(2)}<br/>Paid by {person(m.action.paidBy)}<br/>Split between {m.action.splitBetween.map((x:string)=>person(x)).join(', ')}</p><button className="btn" onClick={()=>confirm(m.action)}><Check size={14}/> Confirm expense</button></div>}{m.action?.type==='show_settlement'&&<Link className="btn agent-action" to={`/group/${trip.id}/settlement`}>Open Final Settlement <ArrowRight size={14}/></Link>}{m.action?.type==='analyze_spending'&&<div className="agent-analysis"><b><BarChart3 size={14}/> Spending snapshot</b><span>Total spent <strong>${total.toFixed(2)}</strong></span><span>{trip.expenses.length} recorded expense{trip.expenses.length===1?'':'s'}</span>{biggest&&<span>Largest <strong>{biggest.title} · ${Number(biggest.amount).toFixed(2)}</strong></span>}</div>}{m.action?.confirmed&&<small className="completed"><Check size={13}/> Saved to group</small>}</div>)}{loading&&<div className="bubble loading-bubble"><Loader2 size={15}/> Working…</div>}</div><div className="agent-reset"><button onClick={reset}><RotateCcw size={12}/> Reset conversation</button></div><div className="agent-input"><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Tell me what happened…"/><button onClick={()=>send()} disabled={!input.trim()||loading} aria-label="Send"><Send size={17}/></button></div></div>}
