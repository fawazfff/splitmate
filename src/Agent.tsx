import{useState}from'react';import{Sparkles,Send,Loader2}from'lucide-react';
export default function Agent({trip,onExpenseAdded}:{trip:any,onExpenseAdded:(expense:any)=>void}){const[messages,setMessages]=useState<{role:'agent'|'user',text:string}[]>([{role:'agent',text:`Hey! I'm your Splitmate Agent. Tell me about an expense, ask about balances, or ask me how to settle the trip.`}]);const[input,setInput]=useState('');const[loading,setLoading]=useState(false);const send=async()=>{const text=input.trim();if(!text||loading)return;setInput('');setMessages(m=>[...m,{role:'user',text}]);setLoading(true);try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,trip})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Agent request failed');setMessages(m=>[...m,{role:'agent',text:data.message||'Done.'}]);if(data.action?.type==='add_expense')onExpenseAdded({title:data.action.title,amount:Number(data.action.amount),paid:data.action.paidBy,split:data.action.splitBetween});}catch(e:any){setMessages(m=>[...m,{role:'agent',text:e?.message||'I could not reach the agent right now.'}])}finally{setLoading(false)}};const card={background:'var(--surface,#fff)',border:'1px solid var(--border,#e7e4df)',borderRadius:18,overflow:'hidden',boxShadow:'0 12px 30px rgba(0,0,0,.06)'};return <div style={card}><div style={{display:'flex',alignItems:'center',gap:10,padding:'16px 18px',borderBottom:'1px solid var(--border,#e7e4df)'}}><div style={{width:34,height:34,borderRadius:10,display:'grid',placeItems:'center',background:'#111',color:'#fff'}}><Sparkles size={17}/></div><div style={{flex:1}}><b style={{display:'block'}}>Splitmate Agent</b><small style={{opacity:.6}}>Expense assistant · Base</small></div><small style={{color:'#15803d'}}>● Online</small></div><div style={{padding:16,minHeight:250,maxHeight:380,overflowY:'auto',display:'flex',flexDirection:'column',gap:10}}>{messages.map((m,i)=><div key={i} style={{alignSelf:m.role==='user'?'flex-end':'flex-start',maxWidth:'88%',padding:'10px 12px',borderRadius:14,background:m.role==='user'?'#111':'#f5f3ef',color:m.role==='user'?'#fff':'inherit',lineHeight:1.45,fontSize:14}}>{m.text}</div>)}{loading&&<div style={{alignSelf:'flex-start',padding:'10px 12px',borderRadius:14,background:'#f5f3ef',fontSize:14,display:'flex',gap:7,alignItems:'center'}}><Loader2 size={15} style={{animation:'spin 1s linear infinite'}}/>Thinking…</div>}</div><div style={{padding:'0 16px 10px',display:'flex',gap:7,flexWrap:'wrap'}}>{['Who owes who?','How much have we spent?','How should we settle?'].map(s=><button key={s} onClick={()=>setInput(s)} style={{border:'1px solid #ddd8d0',background:'transparent',borderRadius:999,padding:'7px 10px',fontSize:12,cursor:'pointer'}}>{s}</button>)}</div><div style={{display:'flex',gap:8,padding:12,borderTop:'1px solid var(--border,#e7e4df)'}}><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')send()}} placeholder="Tell me what happened…" style={{flex:1,border:'1px solid #ddd8d0',borderRadius:12,padding:'11px 12px',outline:'none'}}/><button onClick={send} disabled={!input.trim()||loading} aria-label="Send" style={{width:42,border:0,borderRadius:12,background:'#111',color:'#fff',display:'grid',placeItems:'center',cursor:'pointer'}}><Send size={17}/></button></div><style>{'@keyframes spin{to{transform:rotate(360deg)}}'}</style></div>}
